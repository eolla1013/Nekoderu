box: python
build:
  steps:
deploy:
  steps:
    - shinobushiva/elastic-beanstalk-deploy:
        key: $ACCESS_KEY
        secret: $ACCESS_SECRET
        app_name: $APP_NAME
        env_name: $ENV_NAME
        region: ap-northeast-1